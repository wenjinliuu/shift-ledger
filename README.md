# 工时簿

面向综合工时制的个人排班、工时记录与加班费预测工具。采用手机优先设计，可作为 PWA 添加到桌面使用。

## 功能

- 综合工时：月总工时减去当月基本工时，得到加班工时
- 普通月薪：支持工作日、周末和法定节假日倍率
- 自定义“白 / 夜 / 休”循环，从指定生效日向后更新且保留历史排班
- 日历逐日调整班次、工时、请假、节假日和备注
- 预测与实际工时、收入、加班费分别统计
- 年度柱状图、基本工时线和月度预测明细
- JSON 数据备份导出与导入恢复
- 手机端液态玻璃界面和桌面模式

## 数据说明

当前版本的数据保存在浏览器本机，不会上传到服务器。更换设备或清除浏览器数据前，请先在设置页导出 JSON 备份。

公开版本不包含任何个人工资或轮班默认值：底薪默认为 0，班次时长与循环使用通用示例。首次使用时请在设置页和批量排班中填写自己的数据。

## 本地开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

项目使用 Next.js 静态导出，Netlify 发布目录为 `out`。`main` 分支更新后，Netlify 会自动重新构建和发布。

## 免责声明

本工具用于个人记录和预估，最终工时及工资以公司考勤、薪资规则和适用法规为准。
